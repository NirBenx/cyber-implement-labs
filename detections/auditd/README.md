# Auditd Rules

`lab02.rules` reconstructs the supplied `execve`, Nmap `connect`, and curl `connect` rules. It is documentation, not proof of the active host configuration. Validate with `augenrules --check` before loading; loading or restarting Auditd requires explicit owner approval.

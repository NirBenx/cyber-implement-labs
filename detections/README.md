# Detection Configuration

These files are sanitized, reconstructed references based on supplied implementation details. They are not byte-for-byte exports from the active system and contain no secrets. Validate in a non-production context before deployment.

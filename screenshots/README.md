# Screenshot Publishing Checklist

Before adding an image, remove or redact usernames, hostnames, public/transient IP addresses, credentials, tokens, workplace identifiers, browser sessions, unrelated applications, and personal data. Link each approved image from the relevant detection or incident document. Empty category directories are retained with `.gitkeep` files.
